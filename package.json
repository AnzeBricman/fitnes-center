{
  "name": "fitnes-center",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "powershell -NoProfile -Command \"if (Test-Path .next) { Remove-Item -LiteralPath .next -Recurse -Force }; if (Test-Path .next-dev) { Remove-Item -LiteralPath .next-dev -Recurse -Force }; if (Test-Path .next-dev-3002.log) { Remove-Item -LiteralPath .next-dev-3002.log -Force }; if (Test-Path .next-dev-3002.err.log) { Remove-Item -LiteralPath .next-dev-3002.err.log -Force }; if (Test-Path .next-start-3003.log) { Remove-Item -LiteralPath .next-start-3003.log -Force }; if (Test-Path .next-start-3003.err.log) { Remove-Item -LiteralPath .next-start-3003.err.log -Force }\"",
    "dev": "npm run clean && next dev",
    "dev:turbo": "npm run clean && next dev --turbopack",
    "build": "npm run clean && next build",
    "start": "next start",
    "preview": "npm run build && next start -p 3001",
    "lint": "next lint",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "db:seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "next": "^15.0.0",
    "nodemailer": "^6.10.1",
    "pdf-lib": "^1.17.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "stripe": "^18.4.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/nodemailer": "^6.4.17",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "prisma": "^6.6.0",
    "typescript": "^5.0.0"
  }
}
